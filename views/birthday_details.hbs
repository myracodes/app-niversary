<div class="banner-gifts-list">
    <img src="/img/blue-banner.jpg" alt="banner" id="banner">

    <div>
        <h1 class="title-gifts-list">{{birthday.friendName}} {{birthday.friendLastName}}</h1>
    </div>
</div>
{{!-- <h1>{{birthday.friendName}} {{birthday.friendLastName}}'s birthday details</h1> --}}
<div class="my-birthdays">
    <ul class="individual-birthday">
        <li>
            <p>
                <b>Birthday:
                    {{birthday.birthdayDay}}/{{birthday.birthdayMonth}}/{{birthday.birthdayYear}} (DD-MM-YYYY)</b>
            </p>
        </li>
        <li>
            <img class="birthday-picture" src="{{birthday.picture}}" alt="friend picture">
        </li>
        <li>
            <p>
                <b>Gifts: </b>
                {{#each birthday.gifts}}
                {{this.name}} <br>
                {{this.price}}€ <br>
                {{this.Description}} <br>
                Buy it: {{this.purchaseLink}} <br>
                <img src="{{this.Picture}}" alt="Picture">
                {{/each}}
            </p>
        </li>
    </ul>

    <form action="/birthday/{{birthday.id}}/edit" method="GET">
        <button class="button">EDIT</button>
    </form>
    <form action="/birthday/{{birthday.id}}/delete" method="POST">
        <button class="button">DELETE</button>
    </form>